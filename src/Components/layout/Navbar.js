import React from 'react';

const Navbar = () => {
  return (
    <nav className='navbar' style={navStyle}>
      SIDHARTHA SINGH
    </nav>
  );
};

const navStyle = {
  textAlign: 'center',
};

export default Navbar;
